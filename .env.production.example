# Copy to .env.production on the server. Never commit the real password.
POSTGRES_DB=health_assessment
POSTGRES_USER=health_app
POSTGRES_PASSWORD=replace-with-a-long-random-password
# Keep false for a temporary HTTP/IP deployment; set true after HTTPS is enabled.
COOKIE_SECURE=false
